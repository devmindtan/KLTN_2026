# Environment variables cho backup-postgres service (rclone version)

# PostgreSQL connection
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=kltn_db
POSTGRES_USER=admin
POSTGRES_PASSWORD=minhtan2003

# Rclone settings
RCLONE_REMOTE=gdrive
RCLONE_FOLDER=School/KLTN_2026/In-project/Backups

# Backup type: 'full' (schemas + data) or 'schema-only'
BACKUP_TYPE=full

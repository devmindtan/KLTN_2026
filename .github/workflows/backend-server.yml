name: Build & Deploy Backend Server

on:
  push:
    branches: [main, develop]
    paths:
      - "backend/server/**"
      - ".github/workflows/backend-server.yml"
  pull_request:
    branches: [main]
    paths:
      - "backend/server/**"

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Get and bump version
        id: version
        run: |
          # Determine version file and repo based on branch
          if [ "${{ github.ref }}" == "refs/heads/main" ]; then
            VERSION_FILE="backend/server/VERSION.prod"
            REPO_NAME="pro-repo"
          else
            VERSION_FILE="backend/server/VERSION.dev"
            REPO_NAME="dev-repo"
          fi

          CURRENT_VERSION=$(cat $VERSION_FILE)
          chmod +x .github/scripts/version-bump.sh
          NEW_VERSION=$(.github/scripts/version-bump.sh $VERSION_FILE)
          echo "new=v$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "repo=$REPO_NAME" >> $GITHUB_OUTPUT
          echo "version_file=$VERSION_FILE" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./backend/server
          file: ./backend/server/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/${{ steps.version.outputs.repo }}:backend-server-${{ steps.version.outputs.new }}
            ${{ secrets.DOCKER_USERNAME }}/${{ steps.version.outputs.repo }}:backend-server-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Commit version bump
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ${{ steps.version.outputs.version_file }}
          git commit -m "chore: bump backend-server version to ${{ steps.version.outputs.new }}" || echo "No changes"
          git push || echo "Push failed"
